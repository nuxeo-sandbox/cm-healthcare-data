description: Generate claim data

options:
    count:
        type: int
        default: 10
        help: Number of records to create
        required: false
    members:
        type: str
        default: 'data/fake_members.csv'
        help: Member data
        required: false
    providers:
        type: str
        default: 'data/fake_providers.csv'
        help: Provider data
        required: false